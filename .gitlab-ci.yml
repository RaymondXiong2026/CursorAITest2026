stages:
  - verify

default:
  image: alpine:3.20

verify:readme:
  stage: verify
  script:
    - test -f README.md
    - test -s README.md
    - echo "README.md exists and is not empty."
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH
